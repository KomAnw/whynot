import { defaultTheme } from './preloadedThemeState';
import { defaultSprite } from './preloadedModeState';

// Состояние по умолчанию для store.
export const preloadedState = { theme: defaultTheme, mode: { sprite: defaultSprite } };
